sequenceDiagram
    actor U as 学生
    participant F as AI Bottom Sheet
    participant A as AI 服务
    participant D as PostgreSQL/Redis
    participant M as OpenAI 兼容模型
    participant R as 预约服务
    U->>F: 输入日期、时段与偏好
    F->>A: POST /ai/chat/stream
    A->>D: 一次性读取本人状态与座位数据
    A->>M: 单次流式请求
    M-->>A: recommend_seats 工具调用
    A->>D: 核验候选实时可用性
    A-->>F: card 事件
    U->>F: 选择卡片并最终确认
    F->>R: POST /reservations
    R->>D: 信用、冲突与并发校验
    R-->>F: 预约结果
