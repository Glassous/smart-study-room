# 概要设计说明书

**项目名称：智能共享自习室预约系统**

| 文档编号 | SSR-HLD-001 | 版本 | V2.0 |
| --- | --- | --- | --- |
| 编写人 | 赵晨曦（模块一）、叶泳言（模块二）、姜文东（模块三）、邹字（模块四）、王建东（模块五） | 日期 | 2026-09-14 |
| 审核人 | 赵晨曦（imicola，组长）及全体组员 | 日期 | 2026-09-14 |
| 修订 | V2.0 全面重构为 5 大功能模块垂直全栈架构，重塑子系统逻辑结构、包划分与接口契约 | 日期 | 2026-09-14 |

### 编写组与模块分工

| 序号 | 姓名 | 学号 | GitHub | 模块归属 | 概要设计主笔与技术职责 |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 1 | **赵晨曦** | 2404060302 | imicola | **模块一：用户与权限模块** | 组长、总体架构设计、概要设计统稿、用户认证（JWT/黑名单）、RBAC 权限中间件 |
| 2 | **叶泳言** | 2304060322 | Glassous | **模块二：座位与预约模块** | 核心预约业务模型与状态机、三层防超卖互斥方案（Redis 分布式锁+GiST）、自动分配算法 |
| 3 | **姜文东** | 2404060307 | DonGKids | **模块三：信用与候补模块** | 信用奖惩流转与禁约判定、满座候补排队队列与自动递补引擎、站内信推送流转 |
| 4 | **邹字** | 2404060319 | hjsdjuhv8 | **模块四：统计与可视化模块** | 数据聚合流水线与高性能统计 SQL、空间占用热力图矩阵、3D 选座探索、Redis 防雪崩旁路缓存 |
| 5 | **王建东** | 2404060309 | nightsky5819 | **模块五：AI 助手与运维模块** | AI 智能对话与推荐服务、分布式限流中间件、高可用调度 Leader 选举、质量保障体系与自动化测试 |

---

## 目录

- [1．引言](#1引言)
- [2．任务概述](#2任务概述)
- [3．总体设计](#3总体设计)
- [4．接口设计](#4接口设计)
- [5．数据结构设计](#5数据结构设计)
- [6．运行设计](#6运行设计)
- [7．出错处理设计](#7出错处理设计)
- [8．安全保密设计](#8安全保密设计)
- [9．维护设计](#9维护设计)

---

# 1．引言

## 1.1 编写目的

本说明书依据《需求规格说明书》（SSR-REQ-001）编制，从总体上定义智能共享自习室预约系统的体系结构、模块划分、外部/内部接口、数据结构与运行方式，作为后续详细设计与编码工作的直接依据。

预期读者：项目组长、各模块全栈开发人员、质量保障人员、课程设计指导教师。

## 1.2 项目背景

a. 本项目为《系统分析与设计》课程设计课题，由 5 人项目小组自主选题开发，指导教师所在课程组为主管部门；开发团队采用「5 大功能模块垂直全栈分工」模式开展架构设计与工程落地，各模块由责任人端到端闭环开发与验收；

b. 本系统为独立 Web 应用（B/S 架构），通过浏览器向学生与管理员提供服务，不与其他业务系统存在接口依赖；数据库采用自管 PostgreSQL 实例。

## 1.3 定义

| 术语/缩写 | 说明 |
| --- | --- |
| 分层架构 | handler（HTTP 适配）→ service（业务）→ repository（数据访问）的三层组织方式 |
| 仓储模式 | Repository 模式，将数据访问细节封装在独立层 |
| 调度器 | 后台定时任务组件，负责违约扫描、候补递补、签到提醒 |
| 分布式锁 | 基于 Redis 实现的资源互斥锁，保障高并发预约与调度器主备选举 |
| Cache-Aside | 旁路缓存模式，先读缓存，失效查库并回填，数据更新时失效缓存 |
| DSN | Data Source Name，数据库连接串 |
| 迁移脚本 | 声明式 SQL 脚本，管理数据库结构演进 |
| 优雅降级 | Redis 离线时自动切换为纯数据库模式，核心功能持续可用 |
| 优雅关闭 | 收到终止信号后停止接收新请求、等待在途请求完成再退出 |

## 1.4 参考资料

| 编号 | 资料 |
| --- | --- |
| [1] | 《需求规格说明书》SSR-REQ-001 V1.0 |
| [2] | GB/T 8567-2006《计算机软件文档编制规范》 |
| [3] | PostgreSQL 18 / Redis 7 / Gin / Vue 3 官方文档 |

# 2．任务概述

## 2.1 目标

在需求规格说明书确定的 G1–G7 目标下，给出可实现的技术总体方案：前后端分离 + REST API + 关系数据库 + Redis 内存缓存/分布式锁 + 后台调度器，保证功能完整、结构清晰、并发安全且便于分工并行开发与测试。

## 2.2 运行环境

后端采用 Go 1.27、Gin 1.12.0、pgx 5.10.0、golang-jwt 5.3.1、go-redis 9.22.0 与 bcrypt；主数据存储为 PostgreSQL 18，Redis 7 用于缓存、限流、JWT 注销黑名单和分布式协调。前端采用 Vue 3.5.42、Vite 8.2.2、Vue Router 5.3.0、Pinia 4.0.3、Element Plus 2.14.5、Axios 1.20.0、ECharts 6.1.0、GSAP 3.15.0 与 Three.js 0.185.1。AI 助手经 OpenAI Chat Completions 兼容接口调用模型（需支持 SSE 流式与工具调用）。系统通过 Docker Compose 编排 PostgreSQL、Redis 和后端服务。

服务端 Linux/容器化环境 + Go 1.27/Gin + PostgreSQL 18 + Redis 7；客户端现代浏览器；开发期前端 Vite 开发服务器（5173）经 CORS 访问后端（8080）。Redis 与 PostgreSQL 均支持 Docker 容器化运行。

## 2.3 需求概述

| 模块 | 对应需求 | 优先级 |
| --- | --- | --- |
| M1 身份认证 | FR-01/02 | 高 |
| M2 座位资源管理 | FR-03 | 高 |
| M3 座位查询与预约 | FR-04 | 高 |
| M4 智能自动分配 | FR-05 | 高 |
| M5 预约生命周期 | FR-06 | 高 |
| M6 信用分管理 | FR-07 | 中 |
| M7 候补队列 | FR-08 | 中 |
| M8 消息通知 | FR-09 | 中 |
| M9 统计分析 | FR-10 | 中 |
| M10 AI 助手 | FR-11~FR-15 | 扩展 |

## 2.4 条件与限制

1. 教学演示场景：单实例部署，无水平扩展要求，但结构上不得阻碍后续扩展；
2. 无外部消息网关：通知全部为站内通知；
3. 时间基准为服务器时间；
4. 小组 5 人分工开发，模块间接口需先行约定（本文档第 4 章）。

# 3．总体设计

## 3.0 AI 助手总体设计

AI 助手作为模块五（AI 助手与运维模块，负责人：王建东）的核心业务子系统，由前端 Vue 悬浮组件、AI HTTP/SSE 适配层、上下文组装服务、OpenAI 兼容流式客户端和 PostgreSQL 会话仓储构成。上下文服务按 JWT 用户 ID 读取本人资料、信用、预约、候补和通知，并组合房间及座位数据；模型只生成简短文本或调用只读 `recommend_seats` 工具。工具结果经后端检查日期、时段、座位状态和实时占用后才作为卡片发送。最终预约由用户确认并复用预约服务，AI 无业务写权限。

接口包括 `GET/POST /api/ai/conversations`、`GET /api/ai/conversations/:id/messages`、`DELETE /api/ai/conversations/:id` 和 `POST /api/ai/chat/stream`。流式协议事件为 `meta`、`delta`、`card`、`done`、`error`。会话表与消息表按用户外键隔离，消息保留 OpenAI 的 `role/content/tool_calls/tool_call_id` 字段。

【插图位置：diagrams/19_AI助手架构图.mmd —— AI 助手组件与数据流】

【插图位置：diagrams/20_AI选座确认时序图.mmd —— AI 推荐与确定性预约时序】

## 3.1 处理流程

系统总体处理流程为"请求—限流—认证—防重—业务—缓存/落库—调度闭环"：
1. 浏览器发起 HTTPS/HTTP 请求；
2. 接入层 Gin 路由与中间件链：CORS 处理 → 限流中间件（基于 Redis 限制敏感接口频次）→ JWT 认证（含 Redis 黑名单校验）→ RBAC 权限判定；
3. handler 解析与校验参数；
4. service 执行业务规则：
   - 读请求优先通过 `rediscache` 检索 Redis 旁路缓存，命中则极速返回；未命中时查库并防雪崩（随机 TTL）回填；
   - 预约/释放等高并发写请求，优先通过 `redissync` 获取分布式细粒度互斥锁（如 `lock:seat:<id>:<date>`），防止并发超卖，并执行数据库事务；事务提交后主动失效相关房间和统计缓存；
5. repository 参数化访问 PostgreSQL 18，依赖 GiST 排除约束最终兜底；
6. 统一格式返回响应；同时后台调度器通过 Redis 分布式锁完成主备选举，周期运行违约扫描、候补递补与签到提醒。

【插图位置：diagrams/01_总体处理流程图.mmd —— 图 3-1 总体处理流程图】

## 3.2 总体结构和模块外部设计

系统采用前后端分离的垂直全栈切分与水平分层结合架构，集成高性能内存缓存与分布式协调机制：

【插图位置：diagrams/02_系统架构图.mmd —— 图 3-2 系统架构图】

- **表现层（Web UI）**：Vue 3 单页应用（Element Plus + Pinia + ECharts + GSAP + Three.js），按 5 大业务模块划分为登录中心、平面图/3D选座看板、信用与候补视图、运营分析大屏、AI 悬浮球交互；
- **接入层（API Gateway / Middleware）**：Gin 路由与中间件流水线（CORS、Redis 令牌桶/固定窗口限流、JWT 认证与 Redis 注销黑名单拦截、RBAC 角色准入、日志审计）；
- **领域服务层（Domain Services）**：5 大核心全栈业务模块（模块一用户权限、模块二座位预约、模块三信用候补、模块四统计分析、模块五AI运维），内聚各自业务规则、防超卖锁机制与状态机；
- **缓存与协调层（Cache & Distributed Coordination）**：Redis 7，承载房间与统计旁路缓存、细粒度分布式互斥锁、调度器 Leader 选举、注销黑名单与接口限流；
- **持久化存储层（Database）**：PostgreSQL 18 关系数据库，提供 ACID 事务保证、外键级联与 GiST 排除约束（防并发超卖最终防线）。

后端模块结构（包路径）：

【插图位置：diagrams/10_模块结构图.mmd —— 图 3-3 后端模块结构图】

### 5 大功能模块垂直全栈划分与包结构映射表

| 模块归属 | 主责成员 | 前端资产 (`frontend/src/`) | 后端资产 (`backend/internal/`) | 数据资产 (PostgreSQL & Redis) | 核心职责与设计重点 |
|---|:---:|---|---|---|---|
| **模块一：用户与权限模块** | **赵晨曦** | `views/LoginView.vue`<br>`views/ProfileView.vue`<br>`views/admin/AdminView.vue` (用户部分)<br>`stores/auth.js`<br>`api/auth.js`<br>`router/index.js` (路由守卫) | `handler/auth.go`<br>`handler/admin_user.go`<br>`service/auth.go`<br>`middleware/auth.go` (JWT + 黑名单)<br>`repository/user.go`<br>`model/user.go`<br>`model/claims.go` | **数据表**：`users`<br>**Redis 键**：`studyroom:auth:blacklist:<token_hash>` | 组长、总体架构、代码审查与合并、报告统稿；用户模型与 Claims、bcrypt 口令哈希、JWT 鉴权拦截、RBAC 隔离与主动注销黑名单 |
| **模块二：座位与预约模块** | **叶泳言** | `views/BookingView.vue`<br>`views/MyReservationsView.vue`<br>`components/SeatPlanDesk.vue`<br>`stores/reservation.js`<br>`api/room.js`<br>`api/reservation.js` | `handler/room.go`<br>`handler/admin_room.go`<br>`handler/reservation.go`<br>`handler/allocation.go`<br>`service/seat.go`<br>`service/reservation.go`<br>`service/allocation.go`<br>`service/lifecycle.go`<br>`pkg/redissync/*`<br>`repository/room.go`<br>`repository/seat.go`<br>`repository/reservation.go`<br>`model/room.go`<br>`model/reservation.go`<br>`model/allocation.go` | **数据表**：`rooms`, `seats`, `reservations`<br>**约束**：`excl_seat_overlap`, `excl_user_overlap` (GiST)<br>**Redis 键**：`studyroom:lock:seat:*`, `studyroom:cache:rooms` | 核心预约业务模型与六态有限状态机；座位批量生成算法；智能加权偏好推荐模型；三层防超卖互斥方案（Redis 分布式锁 + 冲突预检 + GiST 排除约束） |
| **模块三：信用与候补模块** | **姜文东** | `views/WaitlistView.vue`<br>`views/NotificationsView.vue`<br>`views/ProfileView.vue` (信用面板)<br>`stores/notification.js`<br>`api/credit.js`<br>`api/waitlist.js`<br>`api/notification.js` | `handler/credit.go`<br>`handler/waitlist.go`<br>`handler/notification.go`<br>`service/credit.go`<br>`service/waitlist.go`<br>`service/notification.go`<br>`repository/credit.go`<br>`repository/waitlist.go`<br>`repository/notification.go`<br>`model/credit.go`<br>`model/waitlist.go`<br>`model/notification.go` | **数据表**：`credit_logs`, `waitlist`, `notifications`<br>**复合索引**：`idx_credit_user`, `idx_wait_room_slot`, `idx_notify_user_unread` | 校园自习室规则治理体系；信用分奖惩流转与低分禁约；高并发满座先到先得队列递补引擎；站内通知推送与已读管理 |
| **模块四：统计与可视化模块** | **邹字** | `views/AnalyticsView.vue`<br>`components/seat3d/*` (`SeatExplorer.vue`, `SeatScene.vue`, `models.js`, `seatPresentation.js`)<br>`composables/motion.js`<br>`api/stats.js` | `handler/stats.go`<br>`service/stats.go`<br>`repository/stats.go`<br>`model/stats.go`<br>`pkg/rediscache/*` | **数据视图**：预约历史多维分析聚合<br>**Redis 键**：`studyroom:cache:stats:*` (Jitter ±10% 随机防雪崩) | 数据聚合流水线与高性能统计 SQL；座位 × 小时热力图矩阵计算；Three.js WebGL 3D 选座与视角探索；GSAP 界面动效；旁路缓存防雪崩与主动失效机制 |
| **模块五：AI 助手与运维模块** | **王建东** | `components/AIAssistant.vue`<br>`api/ai.js` | `handler/ai.go`<br>`handler/health.go`<br>`service/ai.go`<br>`service/ai_client.go`<br>`service/scheduler.go`<br>`middleware/ratelimit.go`<br>`repository/ai.go`<br>`model/ai.go`<br>`config/config.go` | **数据表**：`ai_conversations`, `ai_messages`<br>**Redis 键**：`studyroom:scheduler:leader`, `studyroom:ratelimit:*` | AI 悬浮流式对话助手与只读查询；自然语言推荐卡片与二次确认预约；高可用调度器 Redis Leader 选举；API 分布式限流；测试体系与 CI/CD 运维 |

## 3.3 功能分配

系统 15 项功能需求（FR-01 ~ FR-15）与 5 大功能模块及主程序单元映射如下：

| 需求编号 | 功能需求名称 | 所属功能模块 | 主责成员 | 前端交互单元 | 后端核心程序单元 | 数据与缓存单元 |
|:---:|---|---|:---:|---|---|---|
| **FR-01** | 用户注册 | 模块一：用户与权限 | 赵晨曦 | `LoginView.vue`, `api/auth.js` | `handler/auth.go`, `service/auth.go`, `repository/user.go` | `users` 表 |
| **FR-02** | 用户登录与个人中心 | 模块一：用户与权限 | 赵晨曦 | `LoginView.vue`, `ProfileView.vue`, `stores/auth.js` | `handler/auth.go`, `middleware/auth.go`, `service/auth.go` | `users` 表, `studyroom:auth:blacklist:*` |
| **FR-03** | 房间与座位管理 | 模块二：座位与预约 | 叶泳言 | `views/admin/AdminView.vue`, `api/room.js` | `handler/admin_room.go`, `service/seat.go`, `repository/room.go` | `rooms`, `seats` 表, `studyroom:cache:rooms` |
| **FR-04** | 座位查询与在线预约 | 模块二：座位与预约 | 叶泳言 | `BookingView.vue`, `SeatPlanDesk.vue`, `api/reservation.js` | `handler/reservation.go`, `service/reservation.go`, `pkg/redissync` | `reservations` 表, GiST 排除约束, `studyroom:lock:seat:*` |
| **FR-05** | 智能自动分配 | 模块二：座位与预约 | 叶泳言 | `BookingView.vue`, `stores/reservation.js` | `handler/allocation.go`, `service/allocation.go` | `seats`, `reservations` 查询聚合 |
| **FR-06** | 预约生命周期管理 | 模块二：座位与预约 | 叶泳言 | `MyReservationsView.vue`, `api/reservation.js` | `service/lifecycle.go`, `handler/reservation.go`, `pkg/redissync` | `reservations` 表（状态与时间戳） |
| **FR-07** | 信用分管理 | 模块三：信用与候补 | 姜文东 | `ProfileView.vue`, `api/credit.js` | `handler/credit.go`, `service/credit.go`, `repository/credit.go` | `credit_logs` 表, `users.credit_score` |
| **FR-08** | 满座候补队列 | 模块三：信用与候补 | 姜文东 | `WaitlistView.vue`, `api/waitlist.js` | `handler/waitlist.go`, `service/waitlist.go`, `repository/waitlist.go` | `waitlist` 表, 复合索引 `idx_wait_room_slot` |
| **FR-09** | 站内消息通知 | 模块三：信用与候补 | 姜文东 | `NotificationsView.vue`, `stores/notification.js` | `handler/notification.go`, `service/notification.go` | `notifications` 表, `idx_notify_user_unread` |
| **FR-10** | 统计分析与热力图 | 模块四：统计与可视化 | 邹字 | `AnalyticsView.vue`, `components/seat3d/*`, `api/stats.js` | `handler/stats.go`, `service/stats.go`, `pkg/rediscache` | 历史预约视图, `studyroom:cache:stats:*` |
| **FR-11** | AI 对话与本人信息查询 | 模块五：AI 助手与运维 | 王建东 | `components/AIAssistant.vue`, `api/ai.js` | `handler/ai.go`, `service/ai.go`, `service/ai_client.go` | `ai_conversations`, `ai_messages` 表 |
| **FR-12** | AI 统一业务上下文组装 | 模块五：AI 助手与运维 | 王建东 | `components/AIAssistant.vue` | `service/ai.go`, `repository/ai.go` | 业务多表聚合快照读取 |
| **FR-13** | 自然语言座位推荐卡片 | 模块五：AI 助手与运维 | 王建东 | `components/AIAssistant.vue` (卡片组件) | `service/ai.go`, `service/ai_client.go` | 实时空位冲突核验 |
| **FR-14** | 推荐卡片二次确认与预约 | 模块五：AI 助手与运维 | 王建东 | `components/AIAssistant.vue` (确认弹窗) | `service/ai.go` 调用 `service/reservation.go` | 复用模块二锁与事务机制 |
| **FR-15** | 会话持久化与隔离管理 | 模块五：AI 助手与运维 | 王建东 | `components/AIAssistant.vue` (会话列表) | `handler/ai.go`, `repository/ai.go`, `middleware/ratelimit.go` | `ai_conversations`, `ai_messages` 表, `studyroom:ratelimit:*` |

# 4．接口设计

## 4.1 外部接口

**（1）用户界面接口**：前端 SPA 通过 axios 调用后端 REST API；除登录/注册与健康检查外，全部请求携带 `Authorization: Bearer <JWT>`；响应统一为 `{"code", "message", "data"}`。

**（2）软件接口（REST API 5 大模块划分）**：

| 模块归属 | API 前缀 | 主要端点 | 缓存/限流与安全策略 | 主责成员 |
|---|---|---|---|:---:|
| **模块一：用户与权限** | `/api/auth`<br>`/api/admin/users` | register、login、logout、profile、admin/users 列表与状态维护 | login 接口受限流（每 IP 每分钟 10 次）；logout 写入 JWT 黑名单；admin 端点 RBAC 保护 | **赵晨曦** |
| **模块二：座位与预约** | `/api/rooms`<br>`/api/seats`<br>`/api/reservations` | 房间列表、座位平面图、创建预约、自动分配、签到/离开/返回/签退/取消、我的预约 | 手动预约和自动分配受限流（每用户每秒 2 次）；加细粒度分布式互斥锁并失效对应房间缓存 | **叶泳言** |
| **模块三：信用与候补** | `/api/credit`<br>`/api/waitlist`<br>`/api/notifications` | 信用分与流水明细、加入/取消候补、我的候补列表、通知列表、未读数、标记已读 | 事务保证信用与流水一致性；候补与预约解耦流转；通知异步写入 | **姜文东** |
| **模块四：统计与可视化** | `/api/stats` | heatmap、overview、trend、peak、top-seats | 耗时聚合指标采用 Redis 旁路缓存（基础 TTL 3 分钟并带 ±10% 随机抖动防雪崩） | **邹字** |
| **模块五：AI 助手与运维** | `/api/ai`<br>`/api/health` | conversations 列表/新建/消息/删除、chat/stream、健康探针 | 仅 student 角色可访问；chat/stream 每用户每分钟 10 次限流；SSE 事件流式推送 | **王建东** |

**（3）硬件接口**：无专用硬件接口，服务端采用标准 TCP/IP 接口通信。

## 4.2 内部接口与数据边界

### 4.2.1 5 大模块内部接口契约表

后端各模块间通过清晰的 Go Interface 进行解耦与契约调用，明确跨模块边界：

| 提供方模块 | 接口定义与核心方法 | 消费方模块 | 契约说明与跨模块交互规范 |
|---|---|---|---|
| **模块一：用户与权限** | `AuthService`：<br>- `Register(req) (UserPublic, error)`<br>- `Login(req) (TokenResponse, error)`<br>- `Logout(token) error`<br>- `IsTokenBlacklisted(token) bool`<br>- `GetProfile(uid) (UserPublic, error)` | 全局中间件、<br>模块五（AI） | JWT 认证中间件调用黑名单校验合法性；模块五调用获取当前学生资料以拼装对话上下文 |
| **模块二：座位与预约** | `SeatService`：<br>- `ListRooms() ([]Room, error)`<br>- `GetSeatMap(roomId, date, slot) (SeatMap, error)`<br>`ReservationService`：<br>- `Create(req) (Reservation, error)`<br>- `AutoAllocate(req) (AllocResult, error)`<br>- `Cancel/Checkin/Checkout(...) error`<br>`RedisSync`：<br>- `TryLock(key, ttl) (unlockFn, error)` | 模块三（候补）、<br>模块四（统计）、<br>模块五（AI/调度） | 模块三候补释放时调用 Create 创建递补预约；模块五 AI 推荐卡片二次确认调用 Create 预约；模块四读取座位房间数据 |
| **模块三：信用与候补** | `CreditService`：<br>- `ApplyDelta(uid, delta, reason, resId) error`<br>- `GetOverview(uid) (CreditOverview, error)`<br>`WaitlistService`：<br>- `PromoteReleased(roomId, date, slot) error`<br>`NotificationService`：<br>- `Push(uid, type, title, content) error` | 模块二（预约）、<br>模块五（调度器、AI） | 模块二预约签退或违约时回调 ApplyDelta 扣/加分；预约取消/释放触发 PromoteReleased 自动递补；各业务触发 Push 发送站内信 |
| **模块四：统计与可视化** | `StatsService`：<br>- `GetHeatmap(roomId, date) (HeatmapMatrix, error)`<br>- `GetOverview() (StatsOverview, error)`<br>- `GetTrend(days) ([]TrendPoint, error)`<br>`RedisCache`：<br>- `GetOrSet(key, ttl, jitter, loader, dest) error` | 前端大屏、<br>模块二（预约） | 统计服务只读聚合 reservations 历史数据；模块二在预约状态落库后触发 Cache.Delete 剔除过期统计缓存 |
| **模块五：AI 助手与运维** | `AIService` / `AIClient`：<br>- `StreamChat(ctx, uid, convId, msg, sseWriter) error`<br>`Scheduler`：<br>- `ElectLeaderAndRun() error`<br>`RateLimiter`：<br>- `Allow(key, limit, window) (bool, error)` | Gin 中间件、<br>后台调度任务 | 限流中间件拦截全模块敏感端点；调度器每 60 秒竞选 Leader 并依次执行超时违约扫描、候补递补推进与签到提醒 |

### 4.2.2 5 大模块数据边界与持久化/缓存权责表

| 模块归属 | 专属数据库表 (PostgreSQL) | 专属 Redis 键模式 | 数据边界与跨模块访问约束 |
|---|---|---|---|
| **模块一：用户与权限** | `users` | `studyroom:auth:blacklist:<token_hash>` | 用户基础信息唯一属主；其他模块仅通过 `user_id` 外键引用，禁止逆向跨模块直接更新口令或状态字段 |
| **模块二：座位与预约** | `rooms`<br>`seats`<br>`reservations` | `studyroom:lock:seat:<seat_id>:<date>`<br>`studyroom:cache:rooms` | 预约与座位状态唯一属主；拥有 `excl_seat_overlap` 与 `excl_user_overlap` GiST 排除约束；其他模块仅可发起状态流转或只读查询 |
| **模块三：信用与候补** | `credit_logs`<br>`waitlist`<br>`notifications` | 内存辅助队列键（可选） | 信用流水、候补队列与站内通知唯一属主；递补引擎通过数据库事务锁定队首记录，防止并发重复递补 |
| **模块四：统计与可视化** | 历史预约分析视图（聚合查询） | `studyroom:cache:stats:heatmap:<room>:<date>`<br>`studyroom:cache:stats:overview`<br>`studyroom:cache:stats:trend` | 无独立物理表，只读访问历史数据；全权负责统计分析指标的高性能旁路缓存生命周期与 Jitter 随机防雪崩管理 |
| **模块五：AI 助手与运维** | `ai_conversations`<br>`ai_messages` | `studyroom:scheduler:leader`<br>`studyroom:ratelimit:ip:*`<br>`studyroom:ratelimit:user:*` | AI 对话历史唯一属主；全权负责分布式主备选举调度锁与全站 API 限流计数器的生命周期管理 |

# 5．数据结构设计

## 5.1 逻辑结构设计

系统核心实体及联系：

- 用户 User 1—N 预约 Reservation；座位 Seat 1—N 预约；
- 房间 Room 1—N 座位 Seat（级联删除）；
- 用户 User 1—N 信用流水 CreditLog；用户 User 1—N 候补 Waitlist；房间 Room 1—N 候补；
- 用户 User 1—N 通知 Notification；
- 预约 Reservation 1—N 信用流水（一条预约可产生多条流水，如违约扣分）。

【插图位置：diagrams/08_ER图.mmd —— 图 5-1 实体联系图（ER 图）】

## 5.2 物理结构设计

| 对象 | 设计 |
| --- | --- |
| 存储引擎 | PostgreSQL 18 默认堆表 + Redis 7 内存数据结构 |
| 主键 | 全表 BIGSERIAL 代理键 |
| 索引 | idx_res_seat_date、idx_res_user_date、idx_res_status、idx_wait_room_slot、idx_notify_user_unread、idx_seats_room、idx_credit_user、idx_ai_conversations_user_updated、idx_ai_messages_conversation_created 等（见迁移脚本） |
| 约束 | 两项 GiST 排除约束（座位/用户时段不重叠）；枚举 CHECK 约束 |
| 表清单 | 业务表 7 张（users/rooms/seats/reservations/credit_logs/waitlist/notifications）+ AI 表 2 张（ai_conversations/ai_messages） |
| 连接池 | PostgreSQL 使用 pgxpool 默认配置；Redis PoolSize=20、MinIdleConns=5 |
| 缓存键设计 | `studyroom:cache:rooms`、`studyroom:cache:stats:heatmap:<room>:<date>`、`studyroom:cache:stats:overview`、`studyroom:auth:blacklist:<hash>`、`studyroom:lock:user:<uid>:<date>`、`studyroom:lock:seat:<seat>:<date>:<start>_<end>`、`studyroom:scheduler:leader` |

## 5.3 数据结构与程序的关系

| 数据结构 | 对应 model/缓存对象 | 消费程序 |
| --- | --- | --- |
| users 行 | model.User | AuthService、CreditService |
| rooms/seats 行 | model.Room / model.Seat | SeatService、AllocationService |
| reservations 行 | model.Reservation | ReservationService、Scheduler、StatsService |
| waitlist 行 | model.WaitlistEntry | WaitlistService、Scheduler |
| notifications 行 | model.Notification | NotificationService |
| 热力图矩阵 | model.HeatmapMatrix | StatsService → 前端 ECharts |
| ai_conversations/ai_messages 行 | model.AIConversation / model.AIMessage | AIService → AI 助手 |
| 座位平面图缓存 | Redis String (JSON) | SeatService |
| 分布式互斥锁 | Redis String + SETNX + TTL | ReservationService、Scheduler |
| 令牌黑名单 | Redis String + EXPIRE | AuthService、JWTMiddleware |

# 6．运行设计

## 6.1 运行模块的组合

启动组合：`cmd/server` → 加载配置 → 建立 PostgreSQL 连接池 → 建立 Redis 客户端连接池（连通性自检） → 装配 repository → 装配 service（注入 Cache 与 Lock 组件） → 注册路由与限流中间件 → 启动 HTTP 监听 → 启动调度器 goroutine（带主备选举机制）。

运行期并发组合：每个 HTTP 请求一个 goroutine（Gin 管理）；调度器独立 goroutine，每 60 秒执行一轮（基于 Redis 锁竞选 Leader → 违约扫描 → 候补递补 → 签到提醒）。

## 6.2 运行控制

- 配置通过环境变量注入（STUDYROOM_PORT / STUDYROOM_DB_URL / STUDYROOM_JWT_SECRET / STUDYROOM_REDIS_ADDR / STUDYROOM_REDIS_PASSWORD / STUDYROOM_REDIS_DB，以及 AI 相关的 STUDYROOM_AI_BASE_URL / AI_API_KEY / AI_MODEL / AI_CONNECT_TIMEOUT_SECONDS / AI_RESPONSE_TIMEOUT_SECONDS / AI_CONTEXT_MESSAGES）；
- 统一在仓库根目录 `.env` 维护（模板 `.env.example`），后端启动时自动读取，Docker Compose 亦读取同一文件；
- `/api/health` 暴露存活状态（含 PostgreSQL 数据库连通性）；
- 收到 SIGINT/SIGTERM 优雅关闭：停止调度器 → 关闭 HTTP → 释放 Redis 连接 → 释放数据库连接池。

## 6.3 运行时间

调度周期为 60 秒。其他接口响应时间和吞吐能力需在目标部署环境通过专项性能测试确认，本说明书不记录未经复测的数值。

# 7．出错处理设计

## 7.1 出错输出信息

统一响应 `{code, message, data}`；HTTP 状态码与业务场景映射：

| HTTP | 场景 | message 示例 |
| --- | --- | --- |
| 400 | 参数错误 | 开始时间必须早于结束时间 |
| 401 | 未认证/令牌已注销/过期 | 令牌无效、已注销或已过期 |
| 403 | 无权限/禁约/禁用 | 信用分过低，暂时禁止预约 |
| 404 | 资源不存在 | 座位不存在 |
| 409 | 冲突/并发争抢 | 该座位此时段已被预约，或正在被他人预约，请稍后重试 |
| 429 | 频次超限 | 请求过于频繁，请稍后再试 |
| 500 | 内部错误 | 服务内部错误，请稍后重试 |

## 7.2 出错处理对策

1. **事务回滚**：预约创建+信用变动等多表写操作置于同一数据库事务，失败整体回滚；
2. **约束兜底**：分布式互斥锁先行排队并发请求，数据库排除约束最终兜底（409 语义转换）；
3. **Redis 优雅降级（Fallback）**：
   - 启动期如未配置 Redis 或 Redis 宕机，系统自动切换为纯数据库降级模式，并在日志中输出警告，不中断核心业务；
   - 运行期若 Redis 发生瞬时故障，缓存读取失败时相关服务回源数据库；预约锁获取发生 Redis 错误时记录日志并依赖 PostgreSQL 排除约束兜底；认证黑名单查询错误时记录日志并继续校验后的请求；注销操作因无法写入黑名单而返回 503；调度器运行中无法取得 Redis 选主锁时跳过本轮；
4. **降级运行**：统计接口异常不影响预约主链路；通知写入失败仅记日志不阻断主流程；
5. **恢复与再启动**：服务无本地状态，重启即恢复；调度任务幂等（状态条件更新），重复执行无副作用。

# 8．安全保密设计

1. 口令 bcrypt（cost=10）存储，杜绝明文/可逆加密；
2. JWT HS256，密钥经环境变量注入，令牌含 uid/role/过期时间，有效期 24h；
3. JWT 主动注销黑名单：用户退出登录或被封禁时，将 Token 哈希存入 Redis 并设置等于 Token 剩余有效期的 TTL，中间件实时校验，彻底解决 JWT 无法提前作废的安全缺陷；
4. 接口级防刷与限流：敏感端点（如登录、提交预约）挂载 Redis 计数器/滑动窗口中间件，超过阈值直接阻断（429 Too Many Requests），防范暴力破解与恶意抢座；
5. RBAC：`/api/admin/**` 强制 admin 角色，中间件统一拦截；
6. SQL 全参数化（pgx 占位符），杜绝字符串拼接；
7. 日志脱敏：口令字段永不落日志；
8. 开发配置允许任意来源跨域，正式部署前需配置来源白名单。

# 9．维护设计

1. **数据库演进**：backend/migrations 按序号管理，`scripts/apply_migrations.sh` 可重复执行；
2. **结构化日志**：Gin 访问日志 + 业务错误日志，含 trace 摘要；
3. **单元测试**：核心算法（分配/状态机/信用/冲突）单测覆盖，CI 自动执行；
4. **配置集中**：全部环境变量集中在 config 包，变更零散参数无需改代码；
5. **部署脚本**：scripts 目录提供集群初始化/启停、迁移、种子脚本，降低环境维护成本。

【插图位置：diagrams/18_部署图.mmd —— 图 9-1 系统部署图】
